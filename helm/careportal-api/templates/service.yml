apiVersion: v1
kind: Service
metadata:
  name: {{ include "careportal-api.fullname" . }}-svc
  labels:
    app: {{ include "careportal-api.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "careportal-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
